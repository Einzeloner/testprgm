// A random C  program for heirarchy things

# include <stdio.h>

int  main() {
    int a1, a2, a3, a4, a5; 
    printf( " Enter a 5 digit int number: " );
    scanf("%d%d%d%d%d", &a1, &a2, &a3, &a4, &a5);

    printf("Sum: %d", a1 + a2 + a3 +a4 + a5);
}